@external propSource highlighting from "./highlight.js"

@top Program { statement* }

@skip { LineComment | BlockComment }

statement {
	VariableName
}

VariableName { properVariable }

@tokens {
	LineComment { "#" ![\n]* }
	BlockComment {
		"(" ![)]* ")"
		|
		"[" ![\]]* "]"
		|
		"{" ![}]* "}"
	}
	_ { $[ \t] }
}

@external tokens matchProperVariable from "./tokens" { properVariable }

@detectDelim
